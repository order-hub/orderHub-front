import React from "react";
import Layout from "../../components/layout/Layout";

const OrderView = () => {
    return(
        <Layout>
            <div>발주 확인</div>
        </Layout>
    );
};

export default OrderView;