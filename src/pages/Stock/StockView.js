import React from "react";
import Layout from "../../components/layout/Layout";

const StockView = () => {
    return(
        <Layout>
            <div>재고 확인</div>
        </Layout>
    );
};

export default StockView;