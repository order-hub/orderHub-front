import React from "react";
import Layout from "../../components/layout/Layout";

const ProductView = () => {
    return(
        <Layout>
            <div>상품 조회</div>
        </Layout>
    );
};

export default ProductView;