import React from "react";
import Layout from "../../components/layout/Layout";

const ProductSearch = () => {
    return(
        <Layout>
            <div>상품 검색</div>
        </Layout>
    );
};

export default ProductSearch;